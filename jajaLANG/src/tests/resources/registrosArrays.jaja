diver f(&a -> ent) -> ent {
    devuelve @a;
}

diver tronco() {
    ent @ punt = reservar(capacidad(ent));
    @punt = 10;
    ent b = f(punt);
    escribeEnt(b); //Tiene que dar 10
    liberar(punt);
}
