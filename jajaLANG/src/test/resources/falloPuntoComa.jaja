diver tronco() {
    si a {
        devuelve facto;
    }
    sino | {
        devuelve fake;
    }
    sino {
        devuelve impar(x-1);
    }

    //Imprime algo por pantalla
    r::prueba();
    
    //Falta punto y coma
    escribirEntero();

    para i = 0 -> 10 {
        escribirEntero(i);
    } 
}
